# Android / Gradle
.gradle/
build/
build-*/
local.properties
android/local.properties
android/.kotlin/
android/app/.cxx/
*.iml
.idea/

# NDK / native build
android/.cxx/
*.o
*.so.tmp

# Node (upstream tooling)
node_modules/

# pi sessions / scratch
.pi/
*.session.json

# qwen dispatch live logs
tasks/logs/
.qwen-logs/

# isolated dispatch worktrees
.worktrees/

# release secrets — never commit these
.secrets
upload.jks
secrets/
